import { Routes } from '@angular/router';
import { ProofSubmissionComponent } from './proof-submission-form/proof-submission.component';

export const PROOF_SUBMISSION_ROUTES: Routes = [
  { path: '', component: ProofSubmissionComponent }
];

import { Routes } from '@angular/router';
import { TaxComputationSummaryComponent } from './tax-computation-summary/tax-computation-summary.component';

export const TAX_COMPUTATION_ROUTES: Routes = [
  { path: '', component: TaxComputationSummaryComponent }
];
